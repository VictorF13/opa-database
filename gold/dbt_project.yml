name: "opa_gold"
version: "1.0.0"
config-version: 2

profile: "opa_gold"

model-paths: ["models"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  opa_gold:
    +materialized: table
